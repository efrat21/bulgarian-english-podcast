# Requirements for knigovishte-podcast
# Install with: pip install -r requirements.txt
#
# Frozen versions as of 2026-04-17
# stdlib modules (urllib, html.parser, re, argparse, dataclasses, pathlib, os) are excluded

certifi==2023.11.17
idna==3.6
python-dotenv==1.2.2
pyttsx3==2.99
requests==2.31.0
urllib3==2.0.7